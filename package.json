{
  "name": "sg-challenge-5",
  "version": "1.0.0",
  "description": "",
  "devDependencies": {
    "jest": "^28.1.2",
    "serverless-offline": "^8.8.0"
  },
  "dependencies": {
    "dynamodb-toolbox": "^0.4.3"
  },
  "scripts": {
    "deploy-dev": "serverless deploy --stage dev --verbose",
    "deploy-prod": "serverless deploy --stage prod --verbose",
    "local": "serverless offline --stage dev --httpPort 4000",
    "test": "jest"
  }
}
