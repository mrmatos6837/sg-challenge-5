{
  "name": "sg-challenge-5",
  "version": "1.0.0",
  "description": "",
  "devDependencies": {
    "jest": "^28.1.2",
    "serverless-offline": "^8.8.0"
  },
  "dependencies": {
    "dynamodb-toolbox": "^0.4.3"
  },
  "scripts": {
    "local": "serverless offline",
    "test": "jest",
    "deploy-api": "sh ./deploy-api.sh",
    "deploy-db": "sh ./deploy-db.sh",
    "deploy-everything": "npm run deploy-db && npm run deploy-api"
  }
}